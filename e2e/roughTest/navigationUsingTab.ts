// Navigate using TAB

// const ele: ElementFinder = await getDynamicElement(
//     "xpath",
//     `//div[@class='gadget-title' and normalize-space(.)='${gadget}']/ancestor::ul[1]//a[@title='Minimize Gadget']`,
// );
// await ele.sendKeys(protractor.Key.chord(protractor.Key.SHIFT, protractor.Key.TAB));
// await browser.actions().sendKeys(protractor.Key.ENTER).perform();